apiVersion: apps/v1
kind: Deployment
metadata:
  name: motd-deployment
  labels:
    app: motd
spec:
  replicas: 3
  selector:
    matchLabels:
      app: motd
  template:
    metadata:
      labels:
        app: motd
    spec:
      containers:
      - name: motd
        image: magnorod/motd-api-image:2ddded380d8d24f3d40c66d7b0ded84d4f1bffdb
        command: ["./motd-api.py"]
        args: ["80", "serveur web dans mon pod"]
        ports:
        - containerPort: 80
        env:
          - name: REDIS_URL
            value: "10.44.1.236"